import {ColorScheme} from "@/interfaces/ColorScheme";

// darkmode instance of ColorScheme interface
export class Darkmode implements ColorScheme {
    background: string = '#161616';
    textColor: string = 'white';

}
