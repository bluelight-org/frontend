// interface for color schemes
export interface ColorScheme {
  background: string;
  textColor: string;
  cardColor: string;
  inputColor: string;
  inputBorderColor: string;
  buttonColor: string;
}
