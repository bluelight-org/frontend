export interface TranslationInterface {
  common: {
    password: string;
    address: string;
    city: string;
  };
  routes: {
    dashboard: string;
    alert: string;
  };
  alert: {
    missionKeyword: string;
    stabWound: string;
  };
}
